"""check result species

Revision ID: 2f2481b79a93
Revises: 8dc733f2bb2d
Create Date: 2026-09-20 21:11:39.646153
"""
from collections.abc import Sequence

from alembic import op
import sqlalchemy as sa


revision: str = '2f2481b79a93'
down_revision: str | None = '8dc733f2bb2d'
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    # A server default is needed to add a NOT NULL column to a table that already
    # has rows; everything written before this migration was deterministic. The
    # default is then dropped, so the application stays the only thing that
    # decides a result's species.
    op.add_column(
        "check_result",
        sa.Column(
            "species",
            sa.String(length=20),
            nullable=False,
            server_default="deterministic",
        ),
    )
    op.alter_column("check_result", "species", server_default=None)


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column('check_result', 'species')
    # ### end Alembic commands ###
